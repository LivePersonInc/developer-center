https://developers.liveperson.com:
  type: upstream
  upstream: jekyll_app:http

https://{default}/:
  # [...]
  redirects:
    paths:
      "/https://{default}/events.com": { "to": "https://{default}/events.html" }

{
    "type": "object",
    "properties": {
        "ringId": {
            "type": "string"
        },
        "ringState": {
            "type": "string",
            "enum": ["ACCEPTED", "REJECTED"]
        }
    },
    "required": ["ringId", "ringState"]
}
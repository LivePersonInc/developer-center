{
    "title": "RichContent",
    "type": "object",
    "properties": {
        "type": {
            "type": "string",
            "default": "RichContent",
            "enum": ["RichContent"],
            "readonly": "true"
        },
        "message": {
            "type": "object",
            "title" : "element",
            "$ref": "richContent/element.json"
        }
    },
    "required": ["type", "message"]
}
